name: B
on: push
jobs:
  b:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-java@v4
      with:
        java-version: 17
        distribution: temurin
    - run: |
        curl -sL https://githubusercontent.com > /dev/null
        echo "Lệnh build rút gọn chạy tại đây"
